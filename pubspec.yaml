name: app_write_fun
description: Appwrite functions for deleting documents across database collections.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dart_appwrite: ^13.0.0
  http: ^1.2.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
